import React from 'react';
import { PlaceholderScreen } from '../components/PlaceholderScreen';

export const AskScreen: React.FC = () => {
  return (
    <PlaceholderScreen
      eyebrow="Ask"
      title="Controlled Q&A lives here."
      body="This shell is reserved for Ask Stellium contexts tied to either a relationship analysis or the user's romantic profile."
    />
  );
};
